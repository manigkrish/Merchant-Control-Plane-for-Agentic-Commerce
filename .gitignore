# OS / IDE
.DS_Store
.idea/
*.iml
.vscode/

# Java / Maven
target/
*.log

# Env files / secrets
.env
.env.*
*.pem
*.key
*.p12
*.jks
*.keystore

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Local infra / data
data/
tmp/

# Local-only secrets / keystores (must never be committed)
.local/
.local/**

# --- Environment files (never commit secrets) ---
.env
.env.*
*.env
*.env.*
!.env.example
!*.env.example
!infra/docker-compose/.env.example

# --- Windows "Zone.Identifier" metadata (never commit) ---
*:Zone.Identifier
